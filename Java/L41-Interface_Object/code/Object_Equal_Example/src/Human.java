public class Human {
    public int age; // открытое поле класса
    public boolean is_worker; // работает ли человек / открытое поле класса

    public Human(int age, boolean is_worker) {
        this.age = age;
        this.is_worker = is_worker;
    }
}
